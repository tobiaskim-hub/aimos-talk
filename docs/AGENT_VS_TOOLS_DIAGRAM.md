# 🤖 Agent vs Tools 구조 설명

## 📊 전체 구조

```
사용자 요청
    ↓
┌────────────────────────────────┐
│    AI Agent (1개)              │  ← 두뇌
│                                │
│  1. 요청 분석                   │
│  2. 도구 선택 (LLM에게 물어봄)  │
│  3. 도구 실행                   │
│  4. 결과 요약                   │
└──────────┬─────────────────────┘
           │
           │ 도구 호출
           │
    ┌──────▼──────┐
    │   Tools     │  ← 손 (여러 개)
    │   (8개)     │
    └──────┬──────┘
           │
    ┌──────┼──────┬──────┐
    │      │      │      │
  파일   명령어   Git   미래
  도구   도구    도구   도구
  (4개)  (4개)  (포함) (+37개)
```

---

## 💬 실제 대화 예시

### 예시 1: 파일 읽기

```bash
👤 You: README.md 파일을 읽어줘

       ↓
┌─────────────────────────────────────┐
│  🤖 Agent (src/agent.ts)            │
│                                      │
│  Step 1: LLM에게 물어보기            │
│  "사용자가 파일을 읽으려고 해.        │
│   어떤 도구를 써야 하지?"             │
│                                      │
│  Step 2: LLM 답변                    │
│  {                                   │
│    "tool": "read_file",             │
│    "params": ["README.md"]          │
│  }                                   │
│                                      │
│  Step 3: 도구 호출                   │
│  const tool = findTool("read_file") │
│  const result = await tool.execute( │
│    "README.md"                       │
│  )                                   │
└──────────────┬──────────────────────┘
               │
               │ 도구 실행 요청
               ↓
      ┌────────────────────┐
      │  📦 read_file 도구 │  ← 실제 작업 수행
      │  (file-tools.ts)   │
      │                    │
      │  const content =   │
      │    fs.readFile(    │
      │      "README.md"   │
      │    )               │
      │  return content    │
      └─────────┬──────────┘
                │
                │ 결과 반환
                ↓
         "# Ollama AI Agent
          AI 코딩 어시스턴트..."
                │
                │ 결과 요약
                ↓
┌────────────────────────────────────┐
│  🤖 Agent                          │
│                                     │
│  Step 4: LLM에게 결과 요약 요청     │
│  "README.md 파일 내용이야.          │
│   사용자에게 친절하게 설명해줘"      │
└──────────────┬─────────────────────┘
               │
               ↓
     📄 README.md 파일을 읽었습니다!
        총 42줄이며, Ollama AI Agent
        프로젝트 문서입니다.
```

---

## 🔧 실제 코드로 보기

### Agent (src/agent.ts) - 두뇌

```typescript
export class AIAgent {
  private llm: OllamaClient  // LLM 연결
  
  async execute(userRequest: string): Promise<string> {
    // Step 1: LLM에게 물어보기 - 어떤 도구를 쓸까?
    const plan = await this.llm.generate(`
      사용자 요청: "${userRequest}"
      어떤 도구를 사용해야 할까요?
    `)
    
    // Step 2: LLM 답변 파싱
    // plan = { tool: "read_file", params: ["README.md"] }
    
    // Step 3: 도구 찾기
    const tool = findTool(plan.tool)
    
    // Step 4: 도구 실행
    const result = await tool.execute(...plan.params)
    
    // Step 5: 결과 요약
    const summary = await this.llm.generate(`
      결과: ${result}
      사용자에게 친절하게 설명해줘
    `)
    
    return summary
  }
}
```

### Tools (src/tools/file-tools.ts) - 손

```typescript
// 도구 1: 파일 읽기
export const read_file: Tool = {
  name: 'read_file',
  description: '파일을 읽습니다',
  parameters: {
    path: 'string - 파일 경로'
  },
  execute: async (path: string): Promise<string> => {
    const content = await fs.readFile(path, 'utf-8')
    return content  // 실제 작업 수행!
  }
}

// 도구 2: 파일 쓰기
export const write_file: Tool = {
  name: 'write_file',
  description: '파일에 내용을 씁니다',
  parameters: {
    path: 'string - 파일 경로',
    content: 'string - 파일 내용'
  },
  execute: async (path: string, content: string): Promise<string> => {
    await fs.writeFile(path, content, 'utf-8')
    return `✅ 파일 작성 완료: ${path}`  // 실제 작업 수행!
  }
}

// 도구 3: 파일 목록
export const list_files: Tool = { ... }

// 도구 4: 디렉토리 생성
export const create_directory: Tool = { ... }
```

---

## 🆚 Agent vs Tools 비교

| 항목 | Agent | Tools |
|------|-------|-------|
| **개수** | 1개 | 8개 (현재) → 45+개 (목표) |
| **역할** | 두뇌 (생각) | 손 (실행) |
| **파일** | src/agent.ts | src/tools/*.ts |
| **주요 작업** | 도구 선택, 결과 요약 | 실제 작업 수행 |
| **LLM 사용** | ✅ 사용 | ❌ 사용 안 함 |
| **코드 변경** | 거의 안 바뀜 | 계속 추가됨 |

---

## 📈 진화 과정

### 현재 (8개 도구)
```
Agent (1개)
  │
  └── Tools (8개)
       ├── read_file
       ├── write_file
       ├── list_files
       ├── create_directory
       ├── run_command
       ├── git_status
       ├── git_commit
       └── git_log
```

### Phase 1 완료 후 (18개 도구)
```
Agent (1개)  ← 그대로!
  │
  └── Tools (18개)
       ├── [기존 8개]
       ├── edit_file          ← 새로 추가
       ├── multi_edit         ← 새로 추가
       ├── glob_search        ← 새로 추가
       ├── grep_search        ← 새로 추가
       ├── ls_detail          ← 새로 추가
       ├── copy_file          ← 새로 추가
       ├── move_file          ← 새로 추가
       ├── delete_file        ← 새로 추가
       ├── file_info          ← 새로 추가
       └── create_symlink     ← 새로 추가
```

### Phase 4 완료 후 (45+개 도구)
```
Agent (1개)  ← 여전히 1개!
  │
  └── Tools (45+개)
       ├── [파일 도구 15개]
       ├── [명령어 도구 8개]
       ├── [Git 도구 10개]
       ├── [웹 도구 8개]
       └── [기타 도구 4+개]
```

---

## 🎯 핵심 포인트

### ✅ Agent는 1개만!
- **역할**: "어떤 도구를 쓸까?" 결정
- **LLM 사용**: Qwen 2.5:7B
- **코드**: src/agent.ts (거의 안 바뀜)

### ✅ Tools는 계속 추가!
- **역할**: 실제 작업 수행
- **LLM 사용**: 안 함 (순수 코드)
- **코드**: src/tools/*.ts (계속 추가됨)

---

## 💡 비유

### 사람으로 비유하면:

```
Agent = 당신의 뇌 🧠
  - "README를 읽어야 하니까 눈을 사용하자"
  - "파일을 만들어야 하니까 손을 사용하자"
  
Tools = 당신의 신체 기관 👁️👂👃👅✋
  - 눈 (read_file)
  - 손 (write_file)
  - 다리 (run_command)
  - 입 (git_commit)
  - ... 45+개 기관
```

### 회사로 비유하면:

```
Agent = CEO (1명) 💼
  - "고객이 이 작업을 원하네?"
  - "이건 개발팀에게 시키자"
  - "저건 마케팅팀에게 시키자"
  
Tools = 부서들 (여러 개) 👥
  - 개발팀 (파일 도구)
  - IT팀 (명령어 도구)
  - 법무팀 (Git 도구)
  - 홍보팀 (웹 도구)
  - ... 45+개 부서
```

---

## 🔍 GenS/Claude Code Agent도 똑같은 구조!

```
GenS/Claude Agent (1개)
  │
  └── Tools (45+개)
       ├── LS
       ├── Read
       ├── Write
       ├── Edit
       ├── MultiEdit
       ├── Glob
       ├── Grep
       ├── Bash
       ├── WebSearch
       ├── WebFetch
       ├── ... (45+개)
```

**GenS도 Agent 1개 + Tools 45+개 구조입니다!**

---

## 🎯 결론

### ❌ 잘못된 이해
```
각 도구마다 Agent가 있다? (8개 Agent?)
```

### ✅ 올바른 이해
```
Agent는 1개만 있고, 
그 Agent가 45+개의 도구를 사용한다!

Agent (1개) = 두뇌 (도구 선택)
Tools (45+개) = 손 (실제 작업)
```

---

**작성일**: 2026-02-02  
**작성자**: Claude Code Agent  
**핵심**: Agent 1개 + Tools 많이 = 강력한 개발자!
